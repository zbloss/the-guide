Background: The Guide is a tool for Dungeon Masters to help them run their games more efficiently. Currently we allow users to access models for OCR, embedding, or general LLMs through Ollama (or another local running LLM) or through the cloud. I want to allow users to continue using both ollama and cloud models for embeddings if they want to, but by default I want to run this embedding generation locally on the users machine. By default, I want to use the google/embeddinggemma-300m model in this rust project to generate all embeddings and store and retrieve them to and from DuckDB. We'll need to review the available environment variables we have in place to configure models now and may need to clean them up.

Use this PDF to test all final functionality: "C:\Users\altoz\Documents\dnd\Land of Vampires\Land of Vampires Full Campaign.pdf" but know it is extremely large. If you'd like, you can also use this smaller PDF for preliminary testing: "C:\Users\altoz\Downloads\Chalice_of_the_Mountain_God.pdf"

Tasks:
1. Review the existing environment variables we have to power model configurations.
    1. We will need to modify them to make it easier for users to configure whether they want to use a local service like ollama or a cloud model.
    2. Remember, we should use the new embedding process by default, which will run natively in this rest codebase.
2. Design a new feature for the guide that will run an embedding model in Rust to generate embeddings. 
    1. You will need to do some research to determine what is the most reliable package used to run models like this in Rust.
    2. Use accelerators like GPU and MPS when available, but always be able to fall back to CPU.
    3. Use google/embeddinggemma-300m by default.
3. Review the google/embeddinggemma-300m blog post to gain an understanding of how to use it https://huggingface.co/blog/embeddinggemma.
    1. Pay special attention to which prompts should be used for different tasks according to this article.

Goal: Create new feature that will enable local embedding generation via rust by default.
