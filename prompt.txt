Background: Now that we have migrated to using local embedding models within our rust code, moved our vectors from qdrant to duckdb, and moved off of sqlite onto duckdb, we now have some lingering legacy code and bugs that need to be fixed. I plan on selling this software so having it run cleanly and without bugs is key. Review the bugs that I have found and look for similar ones that can be cleaned up.

Tasks:
1. There is a bug on the frontend which prevents me from being able to delete a the "Trace Test" campaign. 
    1. Clicking the "delete" button does not do anything on the frontend.
    2. Clicking "delete" from the admin page prompts with "Delete campaign + Qdrant collection?". This needs to be updated to only say "Delete campaign?".
2. Uploading a global document yields this error on the frontend "Failed to fetch", and the document is not ingested.
3. The "Edit" button on a campaign only allows me to edit metadata. 
    1. This should allow me to upload campaign documents.
4. The Step 3 Upload Campaign Document tab also yields a "Failed to fetch" error on the frontend without ingesting the document.
    1. There is also an existing bug on this page where after uploading the first document the frontend skips straight to step 4.
    2. This prevents the users from being able to upload any supplemental documents.
5. Step 5 add a character takes the user directly to the campaign page which does contain a character creation tab, but it's all the way at the bottom.
    1. This is extremely confusing for users. We need to improve this workflow.
    2. It may be that this world state component is no longer necessary. Review it's uses and remove if applicable.
    3. Jumping straight to the campaign home page prevents me from continuing on past step 5.

Goal: Clean up the codebase and fix any found bugs.
