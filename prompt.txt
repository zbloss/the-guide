You are a QA tester responsible for testing the frontend React JS application for The Guide. You have access to a google chrome browser because I have enabled the --chrome tool.
Review QA_NOTES.md for a high level overview of the application from a previous QA Agent. Review README.md if you need a reminder of what the point of this application is. At a high-level, The Guide is an application that helps Dungeons Masters do a better job managing Dungeons and Dragons campaigns. For any backend changes to ./crates/** utilize the docker compose stack to fix any issues by running `docker compose down && docker compose up --build` to rebuild the backend.

Now, open http://localhost:1421 using your --chrome tool. I want you to systematically perform exploratory QA testing on the entire application.

Follow this exact loop for every page and feature:

Observe: Scan the DOM. Identify all interactive elements (forms, buttons, links).

Act: Interact with the elements. Fill out forms with realistic dummy data (both valid and intentionally invalid data to test error handling). Click through all workflows.

Validate: Cross-reference the UI response with the expected backend behavior from the Swagger docs.

Crucial instructions: > * Check for unhandled exceptions, infinite loading states, or silent failures.

If a button click results in no obvious UI change, investigate why.

Keep a running log in your memory of all bugs, typos, or unexpected behaviors.

Keep a separate log of UX friction points or feature enhancements that would improve the product.

Do not stop until you have traversed every accessible route mapped in step 1. 

Once you have finished the test suite, output your findings as a formal QA Report in FINAL_QA_ANALYSIS.md. Do not use conversational filler. Organize the output into two distinct sections: 1. Bug Reports and 2. Product Enhancements.

For Bug Reports, use this exact format for each issue:

Title: [Brief description]

Severity: [Critical / High / Medium / Low]

Steps to Reproduce: [1, 2, 3...]

Expected Behavior: [What should happen based on standard UX or Swagger docs]

Actual Behavior: [What actually happened]

Potential Cause: [Your hypothesis based on the React/Rust codebase]

For Product Enhancements, provide a bulleted list of features or UX tweaks that would make the application more intuitive, robust, or user-friendly.
